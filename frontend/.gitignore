# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Env — .env.production holds only the public backend URL (no secrets),
# and must be committed so Cloudflare's CI build bakes in the right API base.
.env
.env.*
!.env.example
!.env.production
