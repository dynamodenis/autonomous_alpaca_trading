# Production build config (Cloudflare Pages).
# Points the dashboard at the Hugging Face Space that hosts the backend.
VITE_API_BASE_URL=https://dynamodenis254-alpaca-trading.hf.space
VITE_POLL_INTERVAL_MS=8000
