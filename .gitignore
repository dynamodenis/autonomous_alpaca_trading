# .gitignore
__pycache__/
*.pyc
.venv/

# Secrets — never commit real env files (.env.example is fine)
.env
.env.local
frontend/.env
frontend/.env.local

# Databases (e.g. accounts.db, George.db, Warren.db)
*.db

# Per-trader memory stores
memory/

# Node / frontend build artifacts
node_modules/
frontend/dist/

community_contributions/