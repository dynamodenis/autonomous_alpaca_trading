# ─────────────────────────────────────────────────────────────
#  AI Trading Floor — Backend environment
#  Copy to `.env` and fill in your values:  cp .env.example .env
# ─────────────────────────────────────────────────────────────

# ── Alpaca brokerage (required) ──────────────────────────────
# Paper-trading credentials from https://app.alpaca.markets/
ALPACA_API_KEY=your_alpaca_api_key
ALPACA_SECRET_KEY=your_alpaca_secret_key
ALPACA_PAPER=true                 # true = paper trading (recommended), false = live

# ── LLM provider (OpenRouter) ────────────────────────────────
# Every trader runs through OpenRouter, so this single key unlocks models from
# OpenAI, DeepSeek, Google, xAI, etc. Get one at https://openrouter.ai/keys.
# Models are configured in trading_floor.py using OpenRouter's "provider/model"
# IDs (e.g. openai/gpt-4.1-mini, deepseek/deepseek-chat).
OPENROUTER_API_KEY=your_openrouter_key

# ── Researcher tools ─────────────────────────────────────────
# Web search for the Researcher sub-agent: https://brave.com/search/api/
BRAVE_API_KEY=your_brave_search_key

# ── Market data (optional) ───────────────────────────────────
# Without Polygon, share prices fall back to random numbers (simulation only).
# POLYGON_PLAN: "" (end-of-day, default) | "paid" (15-min delayed) | "realtime"
POLYGON_API_KEY=
POLYGON_PLAN=

# ── Push notifications (optional) ────────────────────────────
# Pushover for trade-summary notifications: https://pushover.net/
PUSHOVER_USER=
PUSHOVER_TOKEN=

# ── Trading floor behaviour ──────────────────────────────────
RUN_EVERY_N_MINUTES=60                  # trading-cycle frequency in minutes
RUN_EVEN_WHEN_MARKET_IS_CLOSED=false    # true to trade outside market hours
USE_MANY_MODELS=false                   # true = 4 different models, false = all GPT

# ── API / CORS ───────────────────────────────────────────────
# Comma-separated list of allowed frontend origins; "*" allows all (dev only).
FRONTEND_ORIGINS=*
